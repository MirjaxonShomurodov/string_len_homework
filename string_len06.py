def main(s1,s2):
    """
    Given two strings, s1 and s2. Return the shortest length between them.
    Args:
        s1: string
        s2: string
    Returns:
        shortest string
    """
    x1=len(s1)
    x2=len(s2)
    if x1<x2:
        return x1
    else:
        (x2<x1)
        return x2
print(main("salomm","aoos"))